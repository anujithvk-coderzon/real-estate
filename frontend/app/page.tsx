import ListAll from "@/components/public/ListAll";
import PublicFrame from "@/components/public/PublicFrame";

const page = () => {
  return (
    <PublicFrame>
      <ListAll />
    </PublicFrame>
  );
};

export default page;
